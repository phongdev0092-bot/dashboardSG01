services:
  - type: web
    name: tool-kpi-dashboard
    env: python
    buildCommand: "pip install -r backend/requirements.txt && npm --prefix frontend install && npm --prefix frontend run build"
    startCommand: "python backend/main.py"
    envVars:
      - key: PORT
        value: 10000
